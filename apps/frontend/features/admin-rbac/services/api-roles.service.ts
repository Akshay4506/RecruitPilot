import { RoleService } from './roles.service';
import { RoleDTO } from './roles.dto';

export const apiRoleService: RoleService = {
  getAll: async () => {
    throw new Error('Not implemented');
  },
  getById: async (id: string) => {
    throw new Error('Not implemented');
  }
};

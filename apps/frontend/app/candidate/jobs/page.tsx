import { CandidateJobs } from "@/features/candidate-jobs/candidate-jobs";

export const metadata = {
  title: "Job Search | RecruitPilot",
  description: "Discover and apply for your next career opportunity.",
};

export default function CandidateJobsPage() {
  return <CandidateJobs />;
}

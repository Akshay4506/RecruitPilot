import { RecruiterInterviews } from "@/features/recruiter-interviews/recruiter-interviews";

export const metadata = {
  title: "Interviews | RecruitPilot ATS",
  description: "Manage candidate interviews.",
};

export default function RecruiterInterviewsPage() {
  return <RecruiterInterviews />;
}
